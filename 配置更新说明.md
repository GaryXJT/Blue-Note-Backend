# Blue Note Backend 配置更新说明

## 📅 更新时间
2025年5月28日

## 🔄 配置更新内容

### 1. MongoDB 配置更新
- **旧配置**: `mongodb://root:spdlw7qd@blue-note-db-mongodb.ns-h49hpg7e.svc:27017`
- **新配置**: `mongodb://root:ndpvkj55@dbconn.sealoshzh.site:30667/?directConnection=true`
- **数据库名**: `bluenote`

### 2. Redis 配置更新
- **旧配置**: `redis://default:dfwfdwgx@blue-note-redis-db-redis.ns-h49hpg7e.svc:6379`
- **新配置**: `redis://default:6qcbbdwx@dbconn.sealoshzh.site:45216`

### 3. 对象存储配置更新
- **Access Key**: `u6holj03`
- **Secret Key**: `bls9lcm5w4bt6qdw`
- **Internal Endpoint**: `object-storage.objectstorage-system.svc.cluster.local`
- **External Endpoint**: `objectstorageapi.hzh.sealos.run`
- **存储桶名称**: `u6holj03-blue-note`
- **SSL**: `true`

## 📁 更新的文件

### 配置文件
1. `Blue-Note-Backend/backend/config/config.yaml`
2. `Blue-Note-Backend/config.yaml`
3. `Blue-Note-Backend/backend/config/config.go`

### 代码文件
1. `Blue-Note-Backend/backend/main.go` - 移除硬编码的MongoDB连接

## ✅ 验证结果

### 服务状态
- ✅ MongoDB连接成功
- ✅ Redis连接成功
- ✅ 对象存储服务初始化成功
- ✅ 服务正常运行在端口8080
- ✅ 健康检查接口返回正常

### 测试命令
```bash
# 启动服务
./start.sh

# 检查状态
./status.sh

# 健康检查
curl http://localhost:8080/health
```

### 健康检查响应
```json
{
  "database": {
    "mongodb": "ok",
    "redis": "ok"
  },
  "status": "ok",
  "time": "2025-05-28T14:10:48Z"
}
```

## 🚀 部署说明

### 使用脚本部署（推荐）
```bash
# 启动服务
./start.sh

# 查看状态
./status.sh

# 查看日志
tail -f logs/app.log

# 停止服务
./stop.sh
```

### 使用systemd服务部署
```bash
# 安装服务
sudo ./install-service.sh

# 启动服务
sudo systemctl start blue-note

# 查看状态
sudo systemctl status blue-note

# 查看日志
sudo journalctl -u blue-note -f
```

## 🔧 环境变量

项目支持以下环境：
- **production** (默认): 使用Sealos云服务器配置
- **local/development**: 使用本地开发配置

可通过设置 `APP_ENV` 环境变量来切换：
```bash
export APP_ENV=production  # 生产环境
export APP_ENV=local       # 本地开发环境
```

## 📝 注意事项

1. **数据库连接**: 新的MongoDB和Redis连接信息已更新，确保网络可达性
2. **对象存储**: 使用新的存储桶和访问密钥，支持SSL连接
3. **端口配置**: 服务运行在8080端口，确保端口未被占用
4. **日志管理**: 日志文件保存在 `logs/app.log`
5. **进程管理**: PID文件保存在 `logs/app.pid`

## 🔍 故障排除

如果遇到连接问题：

1. **检查网络连接**:
   ```bash
   ping dbconn.sealoshzh.site
   ```

2. **检查端口可达性**:
   ```bash
   telnet dbconn.sealoshzh.site 30667  # MongoDB
   telnet dbconn.sealoshzh.site 45216  # Redis
   ```

3. **查看详细日志**:
   ```bash
   tail -f logs/app.log
   ```

4. **重启服务**:
   ```bash
   ./restart.sh
   ``` 